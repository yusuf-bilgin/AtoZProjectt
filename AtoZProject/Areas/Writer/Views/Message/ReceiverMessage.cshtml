@using EntityLayer.Concrete
@model List<WriterMessage>
@{
	ViewData["Title"] = "ReceiverMessage";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}
@{
	string color = "";
}

<div class="col-lg-12 grid-margin stretch-card">
	<div class="card">
		<div class="card-body">
			<h4 class="card-title">Gelen Kutusu</h4>
			<div class="table-responsive">
				<table class="table">
					<thead>
						<tr>
							<th>#</th>
							<th>Konu</th>
							<th>Gönderen</th>
							<th>Detaylar</th>
						</tr>
					</thead>
					<tbody>
						@foreach (var item in Model)
						{
							@* if (item.WriterMessageId == "Yeni yazı")
							{
								color = "badge badge-danger";
							}
							if (item.Status == "Test yazısı")
							{
								color = "badge badge-warning";
							}
							if (item.Status == "Video")
							{
								color = "badge badge-info";
							}
							if (item.Status == "Project")
							{
								color = "badge badge-success";
							} *@
							<tr>
								<th>@item.WriterMessageId</th>
								<td>@item.Subject</td>
								<td>@(((DateTime)item.Date).ToString("dd-MMM-yyyy"))</td>
								<td><a href="/Writer/Default/AnnouncementDetails/@item.WriterMessageId" class="btn btn-outline-primary">Mesajı Göster</a></td>
							</tr>
						}
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>
